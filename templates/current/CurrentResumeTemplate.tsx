/**
 * Entry point for the `current` full-page resume presentation.
 * Implementation lives in `./sections.tsx`; this file exists as a stable import path.
 */
export { CurrentResumeDocument } from "./sections";
